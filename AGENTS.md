# AGENTS

このファイルを本リポジトリの運用ルールの正本とします。

## 1. 基本運用

- 作業は必ず GitHub Issue を起点にする
- 実装、調査、ドキュメント更新、運用ルール変更を含む要望は、着手前に必ず Issue 化する
- 1 回の作業で扱う主対象 Issue は 1 件に絞る
- 判断理由、仕様変更、保留事項は Issue または Issue コメントに残す

## 2. 工程順序

以下の順序で進める。

1. 要件定義
2. 仕様検討
3. 設計
4. 実装
5. テスト
6. リリース

- 原則として工程を飛ばさない
- 工程を切り替える前に、現工程の受け入れ条件を満たしていることを確認する
- 工程完了は対応するマイルストーン Issue で管理する

## 3. 工程切り替え時の必須作業

- `docs/requirements.md` `docs/specification.md` `docs/design.md` `docs/roadmap.md` `docs/validation-plan.md` を見直す
- 変更があれば同じ作業単位でドキュメントへ反映する
- 変更理由と次工程へ持ち越す論点を関連 Issue に記録する
- 次に着手する Issue を 1 件に絞る

## 4. 言語ルール

- 人の目で確認する成果物はすべて日本語で記載する
- 対象: ドキュメント、GitHub Issue、Issue コメント、PR タイトル、PR 本文、リリースノート、検証記録
- API 名、ライブラリ名、コード識別子は原語のままでよいが、説明本文は日本語で書く

## 5. Blender MCP 開発で必要な方針

- Blender 側機能は Blender アドオンに閉じ込める
- Codex との接点は MCP サーバーに集約する
- 外部 AI サービス連携はアダプタ層で抽象化する
- 危険操作は承認フローを前提にする
- 初期段階では任意 Python の無制限実行を避ける
- 初期段階ではローカル環境前提で設計する

## 6. 実装方針

- 自然言語入力を直接 Blender 実行に流さず、構造化操作へ変換してから扱う
- 実行可能な操作種別は allowlist で管理する
- Blender UI が固まりやすい処理は非同期化または段階実行を検討する
- Blender と MCP サーバーの責務境界を曖昧にしない
- 外部依存が重い処理は可能な限り MCP サーバー側へ寄せる

## 7. GitHub 運用

- 依頼を受けたら、まず既存 Issue を確認する
- 該当 Issue がなければ新規 Issue を作成してから着手する
- ドキュメント更新だけの要望でも Issue を作成する
- 工程ごとの完了条件はマイルストーン Issue に集約する
- 工程完了時は、関連ドキュメント更新確認後にマイルストーン Issue をクローズする
